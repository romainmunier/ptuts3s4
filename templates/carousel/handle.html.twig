{% extends 'base.html.twig' %}

{% block title %}Ajouter{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1 class="heading-stylized">{% if view == "edit" %}Éditer{% else %}Ajouter{% endif %}</h1>
                {{ form(form) }}
                <a class="btn btn-primary"
                   href="{{ path(page) }}">Annuler</a>
            </div>
        </div>
    </div>
{% endblock %}

